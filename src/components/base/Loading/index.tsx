import CircularProgress from "@mui/material/CircularProgress";

const Loading = () => (
    <div className="w-full flex items-center justify-center h-full min-h-[30vh]">
        <CircularProgress />
    </div>
);

export default Loading;
